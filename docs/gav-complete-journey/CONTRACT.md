# Gav complete journey — 2026-09-07
Authorization: owner16397 «Обновляй», then «давай». Implement and release the full audited chain, not another first screen.
Audit: workspace reports/pso-gav-journey-reset/JOURNEY.md. Production baseline7ec6976.
Terminal: one context-bearing relationship from signal/card response through recipient accept and safe place proposal, received proposal first, coherent back/close/reopen; profile/signal/filters/errors/empty states share layout. Both actors and intermediate states verified before same-source release both aliases.
Services: existing social APIs + MapZoneService library/search/preview; no auth change, no migration, no paid provider, no external QA sends. Photos contain and swipe browsing only remain.
Explicit backlog contract change within this release: opt-in history=1 on owned requests GET includes rejected/cancelled, never blocked or excluded-owner records; preserve default active-only and existing contact gating. UI labels closed generically; never invent meeting acceptance. History contacts unavailable. Other-person unavailable contact has honest recovery.
New-place flow reuses explicit private library save command then existing server preview/fingerprint/confirmed send; tell user saved to own places, retry idempotent. No bypass to direct coordinate sharing endpoint.
Acceptance: both sources -> pending -> counterpart accept/retry -> choose new or saved place -> preview/cancel/send/retry -> recipient sees first -> chat bridge/no contact -> back/reload -> close/history. Every UI branch error/empty/loading/denied input preserves context. Mobile320/390 and desktop, Chromium/WebKit; evidence scope excludes physical Telegram and actual messages. Run qa:local + targeted API and UX, CI, source and production smoke both aliases.
