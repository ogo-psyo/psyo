# Gav first entry — local correction, 2026-09-07
Owner screenshot shows duplicated area prompts, filters before results, empty map space and clipped primary action. Objective: one understandable first action, not another product flow.
DoR: existing viewerLocation, locating, accessMessage, signalReason and manual search remain truth; no implicit geolocation, social writes, new persisted data or API changes. Both Gav modes and requests stay accessible. Existing coarse-location service retained.
DoD: without location, no empty map or radius/filter/count prompt; one welcome block with locate/manual alternatives. Manual search inline/back preserves input; unavailable/denied GPS permits manual path. Successful selection restores existing real map/filter/results. 320/390/desktop and 200% text/short viewport actions accessible, keyboard and error/retry intact. Existing candidate browsing and two-user flow remain compatible. First PNG delivered as16385; owner subsequently explicitly requested «давай прод». Release after QA/CI, exact source and both documented alias probes. Rollback current5070845.
